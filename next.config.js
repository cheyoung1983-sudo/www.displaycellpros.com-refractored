// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Absolute path to silence Next.js warning about multiple lockfiles
  outputFileTracingRoot: "C:\\Users\\cheyo\\OneDrive\\Documents\\GitHub\\displaycellpros.com",
  // Enable React strict mode for catching potential issues
  reactStrictMode: true,
  // Future: add redirects, rewrites, security headers here if needed
};
export default nextConfig;
